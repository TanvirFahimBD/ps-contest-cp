#include<bits/stdc++.h>
using namespace std;

int main()
{
    int tm, sp;
    double fs;
    cin >> tm >> sp;
    fs = (tm * sp)/12.0;
    cout << fixed << setprecision(3) << fs << endl;

    return 0;
}
